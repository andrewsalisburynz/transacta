{
  "name": "transacta",
  "version": "1.0.0",
  "description": "Household Spending Tracker - Transaction classification and reporting application",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:all": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build:all": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "test:all": "npm run test --workspace=backend && npm run test --workspace=frontend",
    "test:e2e": "playwright test --config=e2e-tests/playwright.config.ts",
    "test:e2e:ui": "playwright test --config=e2e-tests/playwright.config.ts --ui",
    "test:e2e:report": "playwright show-report e2e-tests/playwright-report",
    "lint:all": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "format:all": "npm run format --workspace=backend && npm run format --workspace=frontend"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "spending-tracker",
    "transaction-classification",
    "machine-learning",
    "graphql",
    "vue3"
  ],
  "author": "",
  "license": "MIT"
}
